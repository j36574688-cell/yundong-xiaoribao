# 運動小日報 v4.1.1

以可正常運作的 v4 基線為基礎的白屏修正版。

## 本版重點
- LocalStorage 讀取全面使用安全解析，舊格式／損壞資料不會直接讓首頁失效。
- LocalStorage 寫入增加例外保護。
- Service Worker 快取版本升級為 `sports-daily-v4.1.1-static`。
- 啟用新 Service Worker 時自動清理舊版 `sports-daily-*` 快取。
- HTML 首屏保留載入 fallback；若 JavaScript 發生 runtime error/unhandled rejection，不再只顯示白頁。
- 保留既有 v4 新聞、Highlights、RSS、PWA、自動測試等功能。

## 部署
此 ZIP 請作為目前 `yundong-xiaoribao` 專案的檔案更新來源。
不要刪除既有 Project 或另外建立不必要的新 Project。
